version: "3.3"
services:
  test_export:
    build: .
    command: Command=export OutputFilename=/data/test.json
    depends_on:
    - metabase 
    volumes:
    - type: volume
      source: data
      target: /data

volumes:
  data:

