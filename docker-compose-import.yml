version: "3.3"
services:
  test_import:
    build: .
    command: Command=import InputFilename=/data/test.json
    depends_on:
    - metabase 
    volumes:
    - type: volume
      source: data
      target: /data


volumes:
  data:

